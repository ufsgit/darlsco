import 'package:flutter/material.dart';

class SampleUserSection extends StatelessWidget {
  const SampleUserSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}