import 'package:flutter/material.dart';

class SampleExpiringEquipments extends StatelessWidget {
  const SampleExpiringEquipments({super.key});

  @override
  Widget build(BuildContext context) {
     return const Scaffold(
      body: Text('Sample Expiring Equipments'),
    );
  }
}