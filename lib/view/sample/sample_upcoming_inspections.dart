import 'package:flutter/material.dart';

class SampleUpcomingInspections extends StatelessWidget {
  const SampleUpcomingInspections({super.key});

  @override
  Widget build(BuildContext context) {
    return   const Scaffold(
      body: Text('Sample Upcoming Inspections'),
    );
  }
}