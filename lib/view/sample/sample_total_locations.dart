import 'package:flutter/material.dart';

class SampleTotalLocations extends StatelessWidget {
  const SampleTotalLocations({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('Sample Total Locations'),
    );
  }
}