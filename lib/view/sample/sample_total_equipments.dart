import 'package:flutter/material.dart';

class SampleTotalEquipmentsScreen extends StatelessWidget {
  const SampleTotalEquipmentsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('SampleTotalEquipmentsScreen'),),
    );
  }
}